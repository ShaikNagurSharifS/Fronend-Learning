function defaultExport() {
    console.log("This is the default export function.");
}

// Exporting the function as the default export of the module
// While importing it will not require curly braces
// While importing, you can rename it to any name you prefer
// While importing, you can also use named imports alongside it
// Example of importing:
// import defaultExport from './path/to/module';   

export default defaultExport;
